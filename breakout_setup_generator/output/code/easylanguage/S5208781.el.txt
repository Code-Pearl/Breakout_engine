{
Generated by Breakout Setup Generator -- strategy_id S5208781
Example code only! NOT actual trading strategy!
THIS CODE IS FOR EDUCATIONAL PURPOSES ONLY. Verify before using.
MARKET: YOUR_MARKET   TIMEFRAME: YOUR_TIMEFRAME   DIRECTION: Long
DNA: prev_low|time_window_open|poi_anchored_stop|long
}

Inputs:
    Fract(0.5), TargetMult(5.0), SessionStart(930), SessionEnd(1530);

Vars:
    POI(0), SPACE(0), myBreakoutLong(0), ATR(0), FILTER(false);

// Point Of Initiation
POI = LowD(1);

// ATR / SPACE (distance from the POI)
ATR = AvgTrueRange(10);
SPACE = Fract * ATR;

// The BreakoutLevel (POI + SPACE)
myBreakoutLong = POI + SPACE;

// Filter
FILTER = Time >= SessionStart and Time < SessionStart + (SessionEnd - SessionStart) / 3;

// Entry Condition
If marketposition = 0 and FILTER
    then buy next bar myBreakoutLong stop;

// Exit
// stop sits at the POI level itself (a real support/resistance line),
// not an ATR multiple -- only the target is tunable
myTarget = TargetMult * ATR;
if marketposition = 1 then Sell next bar at POI stop;
if marketposition = 1 then Sell next bar at EntryPrice + myTarget limit;

// Optional hard backstop -- uncomment and size for your market/point value
// Setstoploss(1500);